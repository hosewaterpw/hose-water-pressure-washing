/**
 * What shows in the instant before the Gallery page appears. Deliberately
 * blank: the page is built in advance and loads immediately, so a spinner
 * would flash up and disappear, which looks like a glitch.
 */
export default function Loading() {
  return null
}
